// invoked in worker
/**
 * 定时任务
 */

//https://github.com/node-schedule/node-schedule

// import crontab from "node-crontab";
// // 1 小时执行一次
// let jobId = crontab.scheduleJob("*/1 * * * *", () => {
//        console.log("It's been 1 minutes!");
// });

/*
import schedule from 'node-schedule';

let jobId = schedule.scheduleJob(' 59 23 * * *', () => {
    //console.log(Date() + ":It's been 2  hours!");
    //调用一个 Action
    think.http("/repositories/update", true); //模拟访问

});

let jobUser = schedule.scheduleJob(' 10 23 * * *', () => {

    think.http("/users/update", true);

});


let jobUpdateUser = schedule.scheduleJob(' 25 23 * * *', () => {

    think.http("/users/updateuser", true);

});*/
